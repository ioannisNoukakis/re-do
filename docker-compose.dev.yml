services:
  rabbitMQ:
    image: rabbitmq:3-management
    container_name: redo-rabbitmq-dev
    environment:
      - RABBITMQ_DEFAULT_USER=root
      - RABBITMQ_DEFAULT_PASS=example
    ports:
      - "5672:5672"
      - "5050:15672"
